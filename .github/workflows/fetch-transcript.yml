name: Fetch YouTube Transcript

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'YouTube video URL or ID'
        required: true
        type: string

jobs:
  fetch-transcript:
    runs-on: ubuntu-latest

    steps:
      - name: Install yt-dlp
        run: |
          pip install yt-dlp

      - name: Extract video ID
        id: extract
        shell: bash
        run: |
          INPUT="${{ github.event.inputs.video_url }}"
          if [[ "$INPUT" =~ ^[A-Za-z0-9_-]{11}$ ]]; then
            VIDEO_ID="$INPUT"
          elif [[ "$INPUT" == *"youtu.be/"* ]]; then
            VIDEO_ID=$(echo "$INPUT" | sed -E 's#.*youtu\.be/([^?&/]+).*#\1#')
          elif [[ "$INPUT" == *"/live/"* ]]; then
            VIDEO_ID=$(echo "$INPUT" | sed -E 's#.*youtube\.com/live/([^?&/]+).*#\1#')
          else
            VIDEO_ID=$(echo "$INPUT" | sed -nE 's#.*[?&]v=([^&]+).*#\1#p')
          fi

          if [[ -z "$VIDEO_ID" ]]; then
            echo "Could not parse video id from input: $INPUT"
            exit 1
          fi

          SAFE_NAME=$(echo "$VIDEO_ID" | tr -cd 'A-Za-z0-9._-')
          echo "video_id=$VIDEO_ID" >> $GITHUB_OUTPUT
          echo "artifact_name=transcript-$SAFE_NAME" >> $GITHUB_OUTPUT
          echo "Extracted video ID: $VIDEO_ID"

      - name: Fetch transcript
        id: transcript
        shell: bash
        run: |
          VIDEO_ID="${{ steps.extract.outputs.video_id }}"
          echo "Fetching transcript for: $VIDEO_ID"

          yt-dlp --write-subs --write-auto-subs --sub-lang en --skip-download --sub-format vtt -o "transcript" "https://www.youtube.com/watch?v=$VIDEO_ID" || true

          if [ -f transcript.en.vtt ]; then
            grep -v "^WEBVTT" transcript.en.vtt | \
            grep -v "^Kind:" | \
            grep -v "^Language:" | \
            grep -v "^[0-9][0-9]:[0-9][0-9]" | \
            grep -v "^\s*$" | \
            grep -v "^NOTE" | \
            sed 's/<[^>]*>//g' | \
            awk '!seen[$0]++' > transcript.txt
            echo "Transcript saved to transcript.txt"
          elif [ -f transcript.en-US.vtt ]; then
            grep -v "^WEBVTT" transcript.en-US.vtt | \
            grep -v "^Kind:" | \
            grep -v "^Language:" | \
            grep -v "^[0-9][0-9]:[0-9][0-9]" | \
            grep -v "^\s*$" | \
            grep -v "^NOTE" | \
            sed 's/<[^>]*>//g' | \
            awk '!seen[$0]++' > transcript.txt
            echo "Transcript saved to transcript.txt"
          else
            echo "No transcript found for video $VIDEO_ID" > transcript.txt
            echo "::warning::No transcript available for this video"
          fi

      - name: Upload transcript as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.extract.outputs.artifact_name }}
          path: transcript.txt
          retention-days: 7

      - name: Output transcript preview
        run: |
          echo "=== TRANSCRIPT PREVIEW (first 100 lines) ==="
          head -100 transcript.txt
          echo ""
          echo "=== FULL TRANSCRIPT LENGTH ==="
          wc -l transcript.txt
